# SON Media

Trang HTML/CSS/JS chạy trực tiếp, tái tạo từ PNG thiết kế được cung cấp.

## Chạy local

Mở `index.html` trong trình duyệt. Không cần cài đặt dependency hoặc chạy server.

Toàn bộ asset thiết kế nằm trong `assets/figma/home/`; dự án không sử dụng URL ảnh bên ngoài.
